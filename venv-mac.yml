name: venv-mac
channels:
  - conda-forge
  - defaults
  - apple
dependencies:
  - python=3.10
  - jupyter
  - pandas
  - numpy
  - matplotlib
  - pip
  - tensorflow-deps
  - pip:
      - tensorflow-macos
